\<evalcmd\> Pow && double && Value of `base` raised to the power of `exp`. && base && double && Base value. && exp && double && Exponent. \</evalcmd\>

Computes the value of `base` raised to the power of `exp`.
