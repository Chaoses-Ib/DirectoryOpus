\<evalcmd\> Atan && double && Arc tangent of the input value. && value && double && Input value in radians. \</evalcmd\>

Computes the arc tangent of \<ib:inline-code\>`value`\</ib:inline-code\> (measured in radians).
