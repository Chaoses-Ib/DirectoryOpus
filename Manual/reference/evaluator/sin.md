\<evalcmd\> Sin && double && Sine of the input value. && value && double && Input value in radians. \</evalcmd\>

Computes the sine of \<ib:inline-code\>`value`\</ib:inline-code\> (measured in radians).
